[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify detects Next.js and uses the Essential Next.js plugin.
# Set env vars in Netlify: Site → Build & deploy → Environment (e.g. NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY).
