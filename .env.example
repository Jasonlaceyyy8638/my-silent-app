# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Stripe (for Buy 10 Credits checkout) - https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_...

# Optional: base URL for redirects (defaults to request origin)
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Optional: Chatbase floating chat bubble (get from Chatbase Dashboard → Deploy → Chat widget → Embed)
# NEXT_PUBLIC_CHATBOT_ID=your-chatbot-id
